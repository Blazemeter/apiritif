language: python

matrix:
  include:
  - os: linux
    python: 2.7
  - os: linux
    python: 3.5
  - os: linux
    python: 3.6
#  - os: osx
#    language: generic


install:
- pip install codecov nose nose-exclude jsonpath-rw lxml unicodecsv
script: coverage run -m nose --exclude-dir=tests/resources
after_success:
  - codecov
