language: python

matrix:
  include:
  - os: linux
    python: 3.7
#  - os: osx
#    language: generic


install:
- pip install codecov nose nose-exclude nose-timer jsonpath-rw lxml unicodecsv
script: coverage run --source=. -m nose tests -v --exclude-dir=tests/resources --with-timer --timer-top-n 5
#- coverage run --source=. -m nose tests -v $GENERAL_EXCLUDE $OSX_EXCLUDE --with-timer --timer-top-n 30
after_success:
  - codecov
